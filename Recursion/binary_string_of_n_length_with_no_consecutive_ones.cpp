#include<bits/stdc++.h>
using namespace std;
vector<string> noOfWays(int n){
    if(n==1){return {"1","0"};}
    vector<string> temp;
    vector<string> ans=noOfWays(n-1);
    for(auto s: ans){
        if()
    }
    return ans;
}
int main(){
    string temp="";
    noOfWays(3, temp);
    return 0;
}

do it your own